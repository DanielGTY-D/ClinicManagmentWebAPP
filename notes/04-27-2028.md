# 04-27-2028

## Tasks

1. Specialities

   > Crear la ruta de especialidades, junto con el CRUD
   - se creo la ruta para las especialidades
   -

   > Crear el commit despues de terminar la ruta y sus funcionalidades o separa los commits por cada funcionalidad de la ruta.

   **Response**

   ```json
       {
            id: number,
            createdAt: string,
            updatedAt: string,
           name: string,
           Description: string | null
       }
   ```

   **Request**

   ```json
    {
        Name: string,
        Description: string | null
    }
   ```

   1.1 Crear una solo componente tabla para no repetir codigo
      > Se creo la tabla con el patro compound pero mas simple.

   1.2 Ruta specialities
      > Termine la funcionalidad CRUD para la ruta de especialidades
   
   1.3 Crear los estilos globales para Table y DumbModal
      > 

2. Consulting Rooms
   > Crear la ruta de especialidad, junto con el CRUD
3. Employees
   > Crear la ruta de employee, junto con el CRUD
4. Crear un toast de notificacion global
